module.exports = {
    // Your password, required for authentication
    // Picking a weak password effectively gives attackers access to your entire computer. Make it good.
    password: "password123",

    // The directory in which files are stored
    storage: "C:/Users/<your-username>/filelion",

    // The port to host the server on
    // Use 80 with Admin, or 8080 without
    port: 80
};
